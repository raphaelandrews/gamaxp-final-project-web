import styled from "styled-components";

export const Title = styled.h1`
    font-size: 1.5rem;
    font-weight: 700;
    margin-top: 2rem;
`

export const CartContent = styled.div`
    display: grid;
    gap: 2rem;
    margin: 2rem 0;
`