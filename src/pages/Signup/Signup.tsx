import { SignupForm } from "@/features";
import { Container } from "@/components";

export const Signup = () => {
    return (
        <Container>
            <SignupForm />
        </Container>
    )
};
