import { LoginForm } from "@/features";
import { Container } from "@/components";

export const Login = () => {
    return (
        <Container>
            <LoginForm />
        </Container>
    );
};

